# Switch

Toggle control for switching between on and off states


## Example

import { DocExample } from "@site/src/components/DocExample";

<DocExample name="switch" />


## Switch

Toggle control for switching between on and off states


**Source:** [`packages/appkit-ui/src/react/ui/switch.tsx`](https://github.com/databricks/appkit/blob/main/packages/appkit-ui/src/react/ui/switch.tsx)


### Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `checked` | `boolean` |  | - | - |
| `defaultChecked` | `boolean` |  | - | - |
| `required` | `boolean` |  | - | - |
| `onCheckedChange` | `((checked: boolean) => void)` |  | - | - |
| `asChild` | `boolean` |  | - | - |
| `agentId` | `string` |  | - | Stable id the agent uses to target this switch via the `toggle` tool. Omit to auto-derive one from the accessible label. Only effective inside an `&lt;AgentToolsProvider&gt;`. |



### Usage

```tsx
import { Switch } from '@databricks/appkit-ui';

<Switch /* props */ />
```

