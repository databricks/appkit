# Function: toPlugin()

```ts
function toPlugin<T, U, N>(plugin, name): ToPlugin<T, U, N>;
```

Defined in: [appkit/src/plugin/to-plugin.ts:3](https://github.com/databricks/appkit/blob/main/packages/appkit/src/plugin/to-plugin.ts#L3)

## Type Parameters

| Type Parameter |
| ------ |
| `T` |
| `U` |
| `N` *extends* `string` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `plugin` | `T` |
| `name` | `N` |

## Returns

`ToPlugin`\<`T`, `U`, `N`\>
