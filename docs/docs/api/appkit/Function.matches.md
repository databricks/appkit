# Function: matches()

```ts
function matches(pattern: RegExp): Matcher;
```

Passes when the value matches `pattern`.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `pattern` | `RegExp` |

## Returns

[`Matcher`](TypeAlias.Matcher.md)
