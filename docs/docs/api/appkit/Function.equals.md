# Function: equals()

```ts
function equals(expected: string): Matcher;
```

Passes when the value equals `expected` exactly.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `expected` | `string` |

## Returns

[`Matcher`](TypeAlias.Matcher.md)
