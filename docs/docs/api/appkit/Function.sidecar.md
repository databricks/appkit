# Function: sidecar()

```ts
function sidecar(config: ISidecarConfig): {
  config: ISidecarConfig;
  name: string;
  plugin: typeof SidecarPlugin;
};
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `config` | `ISidecarConfig` |

## Returns

```ts
{
  config: ISidecarConfig;
  name: string;
  plugin: typeof SidecarPlugin;
}
```

### config

```ts
config: ISidecarConfig;
```

### name

```ts
name: string;
```

### plugin

```ts
plugin: typeof SidecarPlugin;
```
