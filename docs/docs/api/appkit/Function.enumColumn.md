# Function: enumColumn()

```ts
function enumColumn(name: string, values: readonly string[]): ColumnBuilder;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `values` | readonly `string`[] |

## Returns

`ColumnBuilder`
