# Function: getCurrentActorId()

```ts
function getCurrentActorId(): string | undefined;
```

The initiating user in a caller scope; no user actor exists in service scope.

## Returns

`string` \| `undefined`
