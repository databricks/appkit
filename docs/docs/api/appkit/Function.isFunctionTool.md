# Function: isFunctionTool()

```ts
function isFunctionTool(t: unknown): t is FunctionTool;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `t` | `unknown` |

## Returns

`t is FunctionTool`
