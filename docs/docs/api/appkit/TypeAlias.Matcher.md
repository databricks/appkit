# Type Alias: Matcher()

```ts
type Matcher = (value: string) => MatchResult;
```

A deterministic matcher: inspects a string value and returns a result.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `string` |

## Returns

[`MatchResult`](Interface.MatchResult.md)
