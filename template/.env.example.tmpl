DATABRICKS_HOST=https://...
{{- if .dotEnv.example}}
{{.dotEnv.example}}
{{- end}}
DATABRICKS_APP_NAME={{.projectName}}
FLASK_RUN_HOST=0.0.0.0
# DATABRICKS_APP_PORT=8000  # Optional: pin the dev port. Default: 8000, falls back to a free port if busy in dev.
