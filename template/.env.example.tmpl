DATABRICKS_HOST=https://...
{{- if .dotEnv.example}}
{{.dotEnv.example}}
{{- end}}
DATABRICKS_APP_NAME={{.projectName}}
FLASK_RUN_HOST=0.0.0.0
# DATABRICKS_APP_PORT=8000  # Optional: preferred dev port (default: 8000). In dev, falls back to a free port if busy.
