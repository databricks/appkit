# Required: Databricks workspace + warehouse used by the analytics plugin.
DATABRICKS_HOST=
DATABRICKS_WAREHOUSE_ID=

# Recommended local auth (personal access token).
# Otherwise the Databricks SDK falls back to its default credential chain.
DATABRICKS_TOKEN=

# Optional verbosity.
APPKIT_LOG_LEVEL=debug
