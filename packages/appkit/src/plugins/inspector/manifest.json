{
  "$schema": "https://databricks.github.io/appkit/schemas/plugin-manifest.schema.json",
  "name": "inspector",
  "displayName": "Inspector Plugin",
  "description": "Opt-in runtime inspector overlay for AppKit apps with prompt generation and localhost bridge forwarding",
  "resources": {
    "required": [],
    "optional": []
  },
  "config": {
    "schema": {
      "type": "object",
      "properties": {
        "enabledByDefault": {
          "type": "boolean",
          "default": false,
          "description": "Render the inspector by default without requiring ?inspect=1."
        },
        "bridgeTarget": {
          "type": "string",
          "default": "http://127.0.0.1:55107/context",
          "description": "Localhost bridge endpoint that receives POST /context payloads."
        },
        "maxForwardPayloadBytes": {
          "type": "number",
          "default": 24000,
          "description": "Maximum serialized payload size forwarded to the local bridge."
        },
        "maxRecentEvents": {
          "type": "number",
          "default": 20,
          "description": "Maximum recent client and server events included in generated bundles."
        },
        "maxStoredSessions": {
          "type": "number",
          "default": 50,
          "description": "Maximum number of inspector sessions stored in memory."
        },
        "maxStoredEventsPerSession": {
          "type": "number",
          "default": 100,
          "description": "Maximum number of recent request events retained per session."
        }
      }
    }
  }
}
