{
  "name": "@databricks-apps/js",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsdown src/index.ts --dts --format=esm --clean && tsc --emitDeclarationOnly",
    "dev": "tsdown --watch ./src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@databricks-apps/types": "workspace:*",
    "apache-arrow": "^21.1.0"
  }
}
