{
  "name": "apps-sdk",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "clean": "pnpm -r exec rm -rf dist",
    "create-package": "NODE_OPTIONS='--import tsx' plop --plopfile tools/create-sdk-package.ts"
  },
  "devDependencies": {
    "plop": "^4.0.4",
    "tsup": "^8.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.6.0"
  }
}
